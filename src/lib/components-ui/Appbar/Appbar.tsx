import { AppBar, styled } from '@mui/material';

const Appbar = styled(AppBar)({
  color: '#000000',
  border: 0,
  boxShadow: '0px 1px 2px -1px rgba(0,0,0,0.2), 0px 2px 2px 0px rgba(0,0,0,0.14), 0px 1px 5px 0px rgba(0,0,0,0.12)',
});

export default Appbar;
