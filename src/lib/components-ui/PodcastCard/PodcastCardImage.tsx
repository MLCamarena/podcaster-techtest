import { styled } from '@mui/material';

const PodcastCardImage = styled('img')({
  width: '80px',
  height: '80px',
  borderRadius: '50%',
  position: 'absolute',
  top: '-40px',
  left: '50%',
  transform: 'translateX(-50%)',
});

export default PodcastCardImage;
