import { Typography, styled } from '@mui/material';

const PodcastCardResponsiveText = styled(Typography)({
  maxWidth: '200px',
  lineBreak: 'auto',
  height: 'auto',
  textAlign: 'center',
});

export default PodcastCardResponsiveText;
